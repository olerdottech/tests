
name: shell commands


on: [push]

jobs:
  run-shell-command
    runs-on:ubuntu-latest

    step:
      - name: checkout
        uses: actions/checkout@v3
      - name: echo string
        run: echo "Hello world"